stages:
  - install
  - test
  - build

install:
  stage: install
  image: node:24-alpine

  before_script:
    - echo "Installing dependencies..."

  script:
    - npm install --omit=dev

test:
  stage: test
  image: node:20-alpine
  needs: ["install"]

  script:
    - npm test